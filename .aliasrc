du='du -sh'
diff='diff -u'
grep='grep --color=auto'
rgrep='grep -r --color=auto'
mk='mkdir -p'
p=popd
o=open
rrm='rm -rf'

check=check.py
django='python manage.py'
t='git t'
ds='git django-shell'
z='workon zamboni'
rmpyc='sudo rm **/*.pyc'
refmem='launchctl unload ~/Library/LaunchAgents/memcached.plist && launchctl load ~/Library/LaunchAgents/memcached.plist'

# ls
ls='ls -G'
l='ls -lh'
l='ls -lh'
ll='ls -lh'
la='ls -A'
lla='la -lhA'


# Arch
pac=pacman
y=yaourt
ins='y -S'
rem='y -Rs'
net='sudo netcfg2 -a; sudo netcfg2'
x=startx

# git
g=git
gd='git diff'
gdc='git diff --cached'
gs='git status -sb'
gci='git commit'
gl='git log'
glp='git log -p'
gv='git svn'
gr='git rebase'
gb='git branch'
gp='git p'
co='git checkout'

# nose
nose=nosetests
n=nose
nh='nose -h M'

# Python
py=python
ip=ipython
ipo=ipython

# vim
vim=/usr/local/bin/vim
v=vim
c=v
b=v

# ssh targets
ucf='ssh je591549@olympus.acs.ucf.edu'
monroe='ssh jbalogh@monroe.cs.ucf.edu'
bp='ssh jeff@basieproject.org'
khan='ssh jbalogh@khan.mozilla.org'
amo=khan
archie='ssh jbalogh@localhost -p2222'

# vim: set ft=sh:
